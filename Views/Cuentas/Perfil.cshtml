

@model IEnumerable<demomvc.Models.Usuario>

@foreach (var item in Model) {
        
               <h1> @Html.DisplayFor(modelItem => item.Usu) </h1>
            
}
<br><br><br>
@foreach (var item in Model) {
        
               <h3> @Html.DisplayFor(modelItem => item.Nombre) </h3>
            
}
<br>
@foreach (var item in Model) {
        
               <h3> @Html.DisplayFor(modelItem => item.Apellidos) </h3>
            
}
<br>
@foreach (var item in Model) {
        
               <h3> @Html.DisplayFor(modelItem => item.Correo) </h3>
            
}
<br>
@foreach (var item in Model) {
        
               <h3> @Html.DisplayFor(modelItem => item.Celular) </h3>
            
}
<br><br><br>
<h4>Añadir Un Roles: </h4>
<br>
<div class="row">
    <div class="col-md-4">
        <a class="btn btn-lg btn-primary btn-block btn-signin" href="Conductor">Conductor</a></div>
    </div>
    <div class="col-md-4">
        
    </div>
    <div class="col-md-4">
        
    </div>



