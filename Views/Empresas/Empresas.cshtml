
@model IEnumerable<demomvc.Models.Empresa>
@{
    ViewData["Title"] = "Index";
}

<h2>Empresas</h2>

<button class="btn btn-lg btn-primary btn-block btn-signin" id="IngresoLog" type="submit" asp-controller="Usuarios" asp-action="#">BUSCAR UNA EMPRESA</button>
<br><br>
<h4>Si no tiene una empresa solo se vera el boton de "CREAR UNA EMPRESA</h4>
<button class="btn btn-lg btn-primary btn-block btn-signin" id="IngresoLog" type="submit"  asp-action="#">CREAR UNA EMPRESA</button>
<br><br>
<h4>Si ya tiene una empresa solo se vera el boton de "VER UNA EMPRESA</h4>
<a class="btn btn-lg btn-primary btn-block btn-signin" href="Empresa">VER EMPRESA</a></div>


<form asp-controller="Empresas" asp-action="Empresas">
    <p>
        Title: <input type="text" name="SearchString">
        <input type="submit" value="Filter" />
    </p>
</form>

<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Empresas" asp-route-id="@item.ID">Details</a> |
            </td>
        </tr>
}
    </tbody>
</table>